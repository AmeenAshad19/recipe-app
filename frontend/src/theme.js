// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff7043',  // Set your custom primary color here
    },
  },
});

export default theme;
